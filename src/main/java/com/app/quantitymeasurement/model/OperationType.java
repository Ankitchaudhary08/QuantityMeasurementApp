package com.app.quantitymeasurement.model;

/**
 * OperationType — Enum for supported quantity measurement operations.
 */
public enum OperationType {
    COMPARE,
    CONVERT,
    ADD,
    SUBTRACT,
    DIVIDE
}
